%h1 Teachers
%table.table
  %thead
    %tr
      %th Name
      %th Email
      %th{colspan: "3"}
  %tbody
    - @teachers.each do |teacher|
      %tr
        %td= teacher.name
        %td= teacher.email
        %td= link_to 'Show', teacher_path(teacher)
        %td= link_to 'Edit', edit_teacher_path(teacher)
        %td= link_to 'Destroy', teacher_path(teacher), method: :delete, data: { confirm: 'Are you sure?' }
