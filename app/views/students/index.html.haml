%h1 Students
%table.table
  %thead
    %tr
      %th Name
      %th Email
      %th{colspan: "3"}
  %tbody
    - @students.each do |student|
      %tr
        %td= student.name
        %td= student.email
        %td= link_to 'Show', student_path(student)
        %td= link_to 'Edit', edit_student_path(student)
        %td= link_to 'Destroy', student_path(student), method: :delete, data: { confirm: 'Are you sure?' }
%br/
